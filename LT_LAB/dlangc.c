#include<stdio.h>
#include<string.h>

int la=0;
char ch;
int state = 0; 

void scan()
{
	while(TRUE)
	{
		switch(state)
		{
			case 0:
		}		
	}
}
int main()
{
	do
	{	
		if(!la)
			scan();
	}while(TRUE);
	return 0;
}
